<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fabiola.monopoly">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelpActivity"
            android:label="Monopoly Help"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".PlayNowActivity"
            android:label="Playing" />
        <activity
            android:name=".PlayerPropertiesActivity"
            android:label="Options" />
        <activity
            android:name=".GridViewPiecesActivity"
            android:label="Choose one piece"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".ManageListActivity"
            android:label="Properties Owner" />
        <activity
            android:name=".ShowPropertyActivity"
            android:label="Property Owner" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".WaitActivity" />
        <activity android:name=".PlayingActivity" />
        <activity android:name=".BuyingActivity" />
    </application>

</manifest>